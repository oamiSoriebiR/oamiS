char* repeat(const char str[], int n)
{
  int str_size = 0;
  
  for(int i = 0; str[i]!='\0'; i++)
  {str_size++;}

  int n_size = n * str_size + 1;

  char n_str[n_size];

  for(int i = 0; i < n; i++)
  {
    for(const char ch : str)
      {
        
      }
  }

}